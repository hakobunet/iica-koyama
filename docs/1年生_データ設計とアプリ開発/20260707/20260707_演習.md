# 1年生｜データ設計とアプリ開発 — 演習 2026/07/07

---

## Part 1｜配列・連想配列

### 問題1-1：配列を集計する

テストの点数が入った配列から、合計・平均・最高点・最低点と「合格者数（70点以上）」を求めてください。

```php
<?php
$scores = [85, 42, 91, 67, 78, 55, 93, 70, 61, 88];

// ① 合計点を出力する

// ② 平均点を出力する（小数点第1位まで）
//    ヒント: number_format($avg, 1)

// ③ 最高点と最低点を出力する

// ④ 70点以上の合格者数を出力する
//    ヒント: array_filter + count
?>
```

---

### 問題1-2：連想配列の配列を操作する

以下の生徒データから条件に合う生徒を抽出・表示してください。

```php
<?php
$students = [
    ["id" => 1, "name" => "田中太郎", "class" => "A", "score" => 85],
    ["id" => 2, "name" => "鈴木花子", "class" => "B", "score" => 62],
    ["id" => 3, "name" => "山田次郎", "class" => "A", "score" => 91],
    ["id" => 4, "name" => "佐藤三郎", "class" => "B", "score" => 47],
    ["id" => 5, "name" => "伊藤美咲", "class" => "A", "score" => 78],
];

// ① 全生徒を「ID:1 田中太郎 (Aクラス) 85点」の形式で出力する

// ② クラスAの生徒だけを出力する

// ③ 80点以上の生徒だけを出力する
?>
```

---

## Part 2｜条件分岐・関数

### 問題2-1：成績評価関数を作る

点数を受け取り、評価文字列を返す関数 `getGrade()` を作ってください。

| 点数 | 評価 |
| --- | --- |
| 90以上 | S |
| 80以上 | A |
| 70以上 | B |
| 60以上 | C |
| 60未満 | D |

```php
<?php
function getGrade(int $score): string {
    // ここに if/elseif を書く

}

// 動作確認
$testScores = [95, 83, 72, 61, 45];
foreach ($testScores as $score) {
    echo "{$score}点 → " . getGrade($score) . "評価\n";
}
// 期待する出力:
// 95点 → S評価
// 83点 → A評価
// 72点 → B評価
// 61点 → C評価
// 45点 → D評価
?>
```

---

### 問題2-2：match 式で年齢区分を判定する

年齢を受け取り、区分を返す関数 `getAgeGroup()` を `match` 式で作ってください。

| 条件 | 区分 |
| --- | --- |
| 12歳未満 | 子ども |
| 18歳未満 | 中高生 |
| 65歳未満 | 大人 |
| 65歳以上 | シニア |

```php
<?php
function getAgeGroup(int $age): string {
    // match(true) と組み合わせて書く

}

echo getAgeGroup(8)  . "\n";  // → 子ども
echo getAgeGroup(15) . "\n";  // → 中高生
echo getAgeGroup(30) . "\n";  // → 大人
echo getAgeGroup(70) . "\n";  // → シニア
?>
```

---

### 問題2-3：税込み価格を計算する関数を作る（発展）

商品の配列を受け取り、`price_with_tax` を追加した配列を返す `addTax()` を作ってください。

```php
<?php
function addTax(array $products, float $taxRate = 0.1): array {
    // array_map を使う

}

$products = [
    ["name" => "ノートPC",   "price" => 98000],
    ["name" => "マウス",     "price" => 3200],
    ["name" => "キーボード", "price" => 8500],
];

$result = addTax($products);
foreach ($result as $p) {
    echo "{$p['name']}: {$p['price_with_tax']}円\n";
}
// 期待:
// ノートPC: 107800円
// マウス: 3520円
// キーボード: 9350円
?>
```

---

## Part 3｜フォームと POST

### 問題3-1：問い合わせフォームを作る

`contact.php` を1ファイルで作成してください。

**仕様：**
- 入力項目：名前・メールアドレス・メッセージ
- 送信後に「〇〇さんからのお問い合わせを受け付けました」と表示
- XSS 対策として `htmlspecialchars()` を必ず使う

```php
<?php
// ▼ contact.php に書こう

$name    = "";
$email   = "";
$message = "";
$sent    = false;

if ($_SERVER["REQUEST_METHOD"] === "POST") {
    // ① データを取り出す（?? "" でnull対策）

    // ② htmlspecialchars を通す

    $sent = true;
}
?>
<!DOCTYPE html>
<html lang="ja">
<head><meta charset="UTF-8"><title>問い合わせ</title></head>
<body>
<?php if ($sent): ?>
  <!-- 送信完了メッセージ -->
<?php else: ?>
  <!-- フォーム -->
<?php endif; ?>
</body>
</html>
```

---

### 問題3-2：バリデーション付きフォームを作る

`register.php` を作成し、以下のバリデーションを実装してください。

**バリデーション条件：**
- 名前：必須・50文字以内
- 年齢：必須・0〜120の整数
- エラー時は赤字で一覧表示し、入力値を保持する

```php
<?php
$errors = [];
$name   = "";
$age    = "";

if ($_SERVER["REQUEST_METHOD"] === "POST") {
    $name = trim($_POST["name"] ?? "");
    $age  = trim($_POST["age"]  ?? "");

    // ① 名前のバリデーション

    // ② 年齢のバリデーション

    // ③ エラーなしなら成功メッセージ
}
?>
<!DOCTYPE html>
<html lang="ja">
<head><meta charset="UTF-8"><title>登録</title></head>
<body>
  <!-- ④ エラーがあれば赤字で表示 -->

  <form method="post" action="">
    <label>名前：<input type="text" name="name" value="<?= htmlspecialchars($name) ?>"></label><br>
    <label>年齢：<input type="number" name="age" value="<?= htmlspecialchars($age) ?>"></label><br>
    <button type="submit">登録</button>
  </form>
</body>
</html>
```
