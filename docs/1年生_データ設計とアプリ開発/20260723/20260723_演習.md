# コマ08 演習｜SQL基礎②（INSERT / UPDATE / DELETE）

`students` テーブル（コマ07・コマ08資料参照）をもとに、以下の問題に答えてください。

---

## 問題1（INSERT）：データを追加する

1. `name` が `'新入生F'`、`age` が 19、`class` が `'B'`、`score` が 70、`enrolled_at` が `'2024-04-01'` の生徒を1件追加する
2. 以下の2名を、1つの `INSERT` 文で同時に追加する
   - `name`: `'新入生G'`, `age`: 18, `class`: `'A'`, `score`: 82
   - `name`: `'新入生H'`, `age`: 20, `class`: `'C'`, `score`: 55

```sql
-- ①

-- ②
```

---

## 問題2（UPDATE）：データを更新する

1. `id` が 3 の生徒の `class` を `'S'` に更新する
2. `class` が `'B'` の生徒全員の `score` に、一律 10点加算する
3. 「もし `WHERE` を付け忘れて `UPDATE students SET score = 0;` を実行してしまったら何が起こるか」を1行で説明する

```sql
-- ①

-- ②
```

（③は文章で回答）

---

## 問題3（DELETE）：データを削除する

1. `id` が 8 の生徒を削除する
2. `score` が 40点未満の生徒を全員削除する
3. 実際に `DELETE` を実行する前に行うべき安全確認のステップを、`SELECT` 文を使って書く（②の対象を確認する想定）

```sql
-- ①

-- ②

-- ③（②の削除対象を事前確認するSELECT）
```

**ヒント：**
- `UPDATE` / `DELETE` は `WHERE` を忘れると全件が対象になってしまうため、実行前に必ず `SELECT` で対象を確認する（資料4章参照）
- 複数行の `INSERT` はカンマ区切りで1文にまとめられる
