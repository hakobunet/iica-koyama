# コマ07 演習｜SQL基礎（SELECT / WHERE / ORDER BY）

---

## 問題1（SELECT基礎）：必要な列だけ取り出す

1. `name` と `class` だけを取得する
2. `name` を「氏名」、`score` を「点数」という別名（AS）で取得する
3. テーブルの全行・全列を取得する

```sql
-- ① name と class だけ取得

-- ② name AS 氏名, score AS 点数 で取得

-- ③ 全カラム・全行を取得
```

---

## 問題2（WHERE）：条件で絞り込む

1. `class` が `'B'` の生徒を取得する
2. `age` が18歳以上、かつ `score` が80点以上の生徒を取得する
3. `class` が `'A'` または `'C'` の生徒を、`IN` を使って取得する
4. `name` に「郎」という文字が含まれる生徒を、`LIKE` を使って取得する

```sql
-- ①

-- ②

-- ③

-- ④
```

---

## 問題3（ORDER BY / LIMIT）：並び替えと件数制限

1. `score` が高い順（降順）に全員を並び替えて取得する
2. 上記のうち、上位3名だけを取得する（`LIMIT` を使う）
3. `class` の昇順、同じ `class` 内では `score` の降順で並び替えて取得する

```sql
-- ①

-- ②

-- ③
```

**期待する②の結果（上位3名）：**
```
渡辺一郎 95点
山田次郎 91点
吉田健太 88点
```

---

## 問題4（集計関数）：全体の統計を求める

1. 生徒の人数を `COUNT(*)` で求める
2. `score` の合計・平均・最高点・最低点を、それぞれ「合計点」「平均点」「最高点」「最低点」という別名で1つのSQLにまとめて取得する

```sql
-- ① 人数を取得

-- ② 合計・平均・最高・最低をまとめて取得
```

---

## 問題5（発展）：WHERE + GROUP BY + ORDER BY を組み合わせる

1. `score` が60点以上の生徒だけを対象に、`class` ごとの平均点（`AVG`）と人数（`COUNT`）を求め、平均点が高い順に並べる
2. クラス `'A'` の生徒の中で、最も点数が高い生徒を1人だけ取得する（`WHERE` + `ORDER BY` + `LIMIT` を組み合わせる）

```sql
-- ① 60点以上の生徒を対象に、class ごとの平均点・人数を平均点の高い順に

-- ② class = 'A' の中で最高得点の生徒を1人だけ
```

**ヒント：**
- ①は `WHERE` → `GROUP BY` → `ORDER BY` の順で書く（実行順序はコマ07のまとめを参照）
- ②は `ORDER BY score DESC LIMIT 1` を使う
